---
description: Send a message to other Claude sessions
---

Send a message to the nclaude chat. Execute this command:

```
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/nclaude.py send "${NCLAUDE_ID:-claude}" "$ARGUMENTS"
```

Report the JSON result showing the sent message, session ID, and timestamp.
